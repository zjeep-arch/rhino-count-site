# 首页 Builds 展示规则

- 项目入口唯一数据源是 `builds-catalog.js`，独立于文章数据 `data.js`。果冻实验室保留在首位，更新日报和文章不得覆盖项目目录。
- Featured Work 只展示 1–2 篇，渲染代码最多取前 2 篇；更多内容通过“浏览全部文章”进入笔记页。不要随着日更文章不断扩展首页侧栏。
- 左侧项目卡片桌面高度为 280px，手机为 260px。标题最多 2 行，摘要最多 3 行，底部入口保持可见；不要靠增加卡片高度容纳长文案。
- 左右栏顶端对齐，各自按内容排布，不允许侧栏拉伸项目卡片。宽屏（1280px 起）3 列，中屏（720px 起）2 列，手机 1 列。
- `index.html` 中的静态内容必须与运行后的内容一致。文章更新不得恢复旧的不限量 Featured Work 渲染方式。
- 发布前执行 `node scripts/check-builds-layout.mjs`。Vercel 构建和 GitHub Pages 发布都会执行此检查；它用 50 篇输入文章验证实际渲染仍只有 2 篇，同时检查果冻入口和项目链接。
- 修改布局时检查 1440px、1024px、390px 下的实际高度、文字截断及按钮可见性，避免仅靠源码判断。
